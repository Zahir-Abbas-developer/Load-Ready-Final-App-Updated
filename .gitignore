# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables (contains Supabase keys — never commit)
.env
.env.local
.env.*.local

# Local tooling (cloudflared binary for sharing dev server)
.tools/

# Local account store (password hashes — never commit)
.data/

# Playwright
test-results/
playwright-report/
blob-report/
playwright/.cache/

# Vitest coverage
coverage/

# Backups. As sensitive as the live store — password hashes, private
# messages, recorded positions. Never in the repository.
backups/
